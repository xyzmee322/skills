---
name: Structured Output
description: > 
  Здесь прописано руководство по Structured Output.
  всегда юзай этот скилл в задачах со Structured Output, ИИ классификациях и когда требуется очень точный ответ.
---

# БАЗА
- json_object говно
- Даём description полям, им можно уделять больше внимания чем систем промпту
- Делаем валидацию не смотря на то что json_schema. В очень редких случаях модель может приколоться и вернуть не JSON
- Чем больше полей с рассуждением тем точнее финальный ответ.

# response_format
Их у моделей всего три:
* json_schema - строго следуем твоей json схеме, всегда юзаем при structured output
* json_object - просто просим вернуть правильный json объект, без утверждённой струтуры, на свой вкус.
* text - ответ без каких-либо рамок  


# Приём: Structured chain-of-thought (думалка внутри схемы)

```

SYSTEM_PROMPT= ("Тебя зовут боб, ты живёшь в телеграм чате где очень много людишек, ты отвечаешь только когда с тобой ведут диалог")
...
...
"response_format": {
    "type": "json_schema",
    "json_schema": {
        "name": "message_routing_logic",
        "strict": True,
        "schema": {
            "type": "object",
            "properties": {
                # 1. Форсим самоидентификацию 
                "self_identity": {
                    "type": "string",
                    "description": "Твоё имя и как тебя могут называть (кратко)."
                },
                # 2. Анализ контекста чата
                "context_analysis": {
                    "type": "string",
                    "description": "К кому именно обращается юзер? Упомянуто ли твоё имя или контекст твоей роли?"
                },
                # 3. Вес
                "confidence_score": {
                    "type": "integer",
                    "description": "Уровень уверенности, что обращаются к тебе (0-100)."
                },
                # 4. Итоговое решение
                "is_addressed_to_me": {
                    "type": "boolean",
                    "description": "Вердикт на основе полей выше"
                }
            },
            # Заставляем вернуть все поля.
            "required": ["self_identity", "context_analysis", "confidence_score", "is_addressed_to_me"],
            # Задаем жесткий порядок ответа.
            "propertyOrdering": ["self_identity", "context_analysis", "confidence_score", "is_addressed_to_me"],
            "additionalProperties": False
        }
    }
}
```

Очень жесткий приём способный сделать тупую модель умной в structured output задачах. Даже без натурального reasoning

LLM - авторегрессионная модель, которая генерит токены последовательно. Фиксируя порядок полей в схеме "propertyOrdering", модель физически сначала пишет текст в поля 'self_identity' и 'context_analysis'. К моменту генерации финального поля 'is_addressed_to_me', эти рассуждения уже лежат в её контексте как свершившийся факт. Модели становится сложнее противоречить самой себе.

ПОЧЕМУ ЭТО КРУТО 😎😎😎 И В РАЗЫ ЛУЧШЕ ЧЕМ ОБЫЧНЫЙ REASONING:
- Мы направляем ход мыслей (сначала вспомни имя, потом проанализируй обращение, потом дай ответ)
- Для тупых моделей Lite это будто единственный способ не галлюцинировать, а опереться на предварительно выведенные факты.
- Дешевле, быстрее и точнее

Короче в классификации со сложным контекстом мастхеф так-то.